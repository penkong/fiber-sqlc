package apirouters

import (
	"github.com/gofiber/fiber/v2"
	// "github.com/penkong/fiber-sqlc/gateway/handlers/auth"
)

func SetUpAuth(r fiber.Router) {
	// authRoutes := r.Group("/auth")
	// authRoutes.Post("/forget-pass", authHandlers.ForgetPassword)
	// authRoutes.Post("/login", authHandlers.Login)
	// authRoutes.Post("/register", authHandlers.Register)
	// authRoutes.Post("/resend", authHandlers.ReSend)
}
