package apirouters

import (
	"github.com/gofiber/fiber/v2"
)

func SetUpUser(r fiber.Router) {
	// userRoutes := r.Group("/user")
	// userRoutes.Post("/current", userHandlers.GetCurrent)
	// userRoutes.Get("/:id", userHandlers.GetUser)
	// userRoutes.Patch("/:id", middleware.Protected(), userHandlers.UpdateUser)
	// userRoutes.Delete("/:id", middleware.Protected(), userHandlers.DeleteUser)
}
